from setuptools import setup, find_packages


setup(
    name='brass_mdl_tools',
    description='Generates a shell MDL file based on the number of TAs',
    author='Austin Whittington and Todd Newton',
    packages=find_packages()
)
